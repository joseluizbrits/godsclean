import { Jost, Open_Sans } from "next/font/google";

export const jost = Jost({
  subsets: ["latin"],
  weight: ["400", "500", "600"],
});

export const openSans = Open_Sans({
  subsets: ["latin"],
  weight: ["400", "600"],
});
